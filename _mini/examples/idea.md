# Subscription Payment Detector

Find recurring subscription payments in user-supplied CSV transaction exports.
The tool should normalize rows, group merchants, detect periodic charges, and
show evidence for each candidate without sending financial data outside the
local workspace.

